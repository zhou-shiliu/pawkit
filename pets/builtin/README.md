# Pawkit built-in pets

`pawkit-sprout` is an original placeholder pet created for Pawkit MVP validation. It is intentionally simple and exists so a fresh install can show a working Codex Pet compatible package without bundling community assets.
